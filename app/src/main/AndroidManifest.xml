<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qhp334.drop">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- //share -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />

    <application
        android:name=".music.APPContext"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RegisterActivity" />
        <activity android:name=".activity.ShareDetailActivity" />
        <activity android:name=".activity.SearchShareActivity" />
        <activity android:name=".activity.TextDetailActivity" />
        <activity android:name=".activity.VideoActivity" />
        <activity android:name=".activity.LMusicActivity" />
        <activity android:name=".activity.CollectActivity" />

        <service android:name=".service.MusicServer" />

        <activity android:name=".activity.OMusicActivity" />
        <activity android:name=".activity.AddShareActivity" />
        <activity android:name=".activity.MyDetailActivity" />
        <activity android:name=".activity.AddTextActivity"></activity>
    </application>

</manifest>